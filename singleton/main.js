let taskHandler = require('./taskHandler')
let myrepo = require('./repo')

myrepo.save('fromMain');
myrepo.save('fromMain');
myrepo.save('fromMain');
myrepo.save('fromMain');
taskHandler.save();
taskHandler.save();
taskHandler.save();
taskHandler.save();

